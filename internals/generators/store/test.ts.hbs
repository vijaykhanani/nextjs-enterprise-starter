import {{pascalCase name}}Store from './index';

describe('{{pascalCase name}}', () => {
  it('checking store properties', async () => {
    const store = new {{pascalCase name}}Store();
    expect(store.Loading).toBe(false);
  });
});
